{
  "name": "bump-tag",
  "version": "0.10.0",
  "description": "Create or move a version tag (and its dependent major/minor tags), then push it",
  "bin": {
    "bump-tag": "run.sh"
  },
  "scripts": {
    "test": "bats test.bats"
  },
  "private": true
}
