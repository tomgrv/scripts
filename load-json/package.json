{
  "name": "load-json",
  "version": "0.18.0",
  "description": "Load JSON from a file or URL, tagging it with $id",
  "bin": {
    "load-json": "run.sh"
  },
  "scripts": {
    "test": "bats test.bats"
  },
  "peerDependencies": {
    "zz_args": "*",
    "zz_colors": "*"
  },
  "private": true
}
