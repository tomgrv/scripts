{
    "name": "zz_wrap",
    "version": "1.0.0",
    "description": "Ensure an env var is set (ask + persist if missing), then run a command",
    "private": true,
    "bin": {
        "zz_wrap": "run.sh"
    },
    "scripts": {
        "test": "bats test.bats"
    }
}
