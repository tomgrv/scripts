{
    "name": "zz_bindir",
    "version": "0.4.0",
    "description": "Resolve (and create if needed) a writable bin directory on PATH",
    "bin": {
        "zz_bindir": "run.sh"
    },
    "scripts": {
        "test": "bats test.bats"
    },
    "private": true
}
