{
  "name": "zz_npx",
  "version": "0.14.0",
  "description": "Run a local node_modules/.bin binary, falling back to npx",
  "bin": {
    "zz_npx": "run.sh"
  },
  "scripts": {
    "test": "bats test.bats"
  },
  "private": true
}
